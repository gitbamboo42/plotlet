"""Vetted, ready-to-use plot recipes — importable after `pip install plotlet`.

Each module here is currently a script-style file with a `__main__` block
(carried over from the previous `examples/` directory). They will be
refactored into importable functions in a follow-up step so callers can do:

    from plotlet.recipes import volcano
    chart = volcano(df, ...)
"""
